// Bu dosyayı kullanarak ProductionOrderForm.tsx dosyasındaki parser fonksiyonlarını düzeltin
// 553. satır için:
parser={(value) => value ? Number(String(value).replace(/\$\s?|(,*)/g, '')) : 0}

// 584. satır için:
parser={(value) => value ? Number(String(value).replace(/\$\s?|(,*)/g, '')) : 0}
